#include "lab9.h"
#include <ctype.h>

// strips whitespace from the left of the string
void rm_left_space(char *s) {
    int i = 0;
    while (isspace(s[i])) {
        i++;
    }

    // TODO: start here
}